# PandoraProject — branch `noctalia`

Post-install Arch (puro) focado em **Umbriel + Noctalia Shell + Greeter**.

→ Ver **[`install/README.md`](install/README.md)**

```bash
sudo ./install/install.sh
```
