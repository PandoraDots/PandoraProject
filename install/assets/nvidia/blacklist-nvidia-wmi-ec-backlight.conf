# Pandora: force intel_backlight on hybrid iGPU sessions.
# nvidia_wmi_ec_backlight often claims the panel but does not drive brightness
# when the compositor runs on Intel.
blacklist nvidia_wmi_ec_backlight
